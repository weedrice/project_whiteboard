import { defineStore } from 'pinia'

export const useNotificationStore = defineStore('notification', () => {
    // This store is now minimal, potentially for global UI state if needed.
    // Data fetching has been moved to useNotification composable (Vue Query).

    return {}
})

name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:
    inputs:
      deploy_backend:
        description: Run the backend validation chain and deploy after the CI gate
        required: false
        type: boolean
        default: false
      deploy_frontend:
        description: Run the frontend validation chain and deploy after the CI gate
        required: false
        type: boolean
        default: false
      allow_contract_migration:
        description: Allow a reviewed contract migration during a manual main deployment
        required: false
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  changes:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      actions: read
      contents: read
      pull-requests: read
    outputs:
      backend: ${{ steps.scope.outputs.backend }}
      frontend: ${{ steps.scope.outputs.frontend }}
      ops: ${{ steps.scope.outputs.ops }}
      backend_deploy: ${{ steps.deploy-scope.outputs.backend }}
      frontend_deploy: ${{ steps.deploy-scope.outputs.frontend }}
      backend_deployment_base_found: ${{ steps.deployment-baselines.outputs.backend_found }}
      backend_deployment_base_sha: ${{ steps.deployment-baselines.outputs.backend_sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Detect validation areas
        id: filter
        uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # v4
        with:
          filters: |
            backend:
              - 'backend/**'
              - 'docs/ops/api-contract-revision.txt'
              # 손으로 고친 스냅샷을 OpenApiSpecSnapshotTest가 잡도록 백엔드도 함께 돈다.
              - 'docs/api/openapi-frontend.json'
              - 'deploy/nginx/**'
              - 'deploy/scripts/activate-backend-release.sh'
              - 'docker-compose.yml'
              - '.github/workflows/ci.yml'
              - '.github/workflows/deploy-backend.yml'
            frontend:
              - 'frontend/**'
              - 'docs/ops/api-contract-revision.txt'
              # 프론트 계약 테스트가 직접 읽는 백엔드 소스. 이 파일만 바뀌는 커밋에서도
              # 프론트 테스트가 돌아야 SSE 이벤트 이름 불일치를 잡을 수 있다.
              - 'backend/src/main/java/com/weedrice/whiteboard/domain/notification/web/NotificationSseEvents.java'
              - 'backend/src/main/java/com/weedrice/whiteboard/domain/file/service/FileUploadTarget.java'
              # 다운로드 헤더 계약 픽스처는 프론트 단위 테스트와 브라우저 E2E가 함께 읽는다.
              - 'backend/src/test/resources/contracts/file-download-content-disposition-utf8.txt'
              # booleanWireNameContract.spec.ts가 읽는 DTO. 어노테이션 위치가 바뀌면
              # wire 이름이 바뀌므로 프론트 테스트가 함께 돌아야 한다.
              - 'backend/src/main/java/com/weedrice/whiteboard/domain/post/dto/PostResponse.java'
              - 'backend/src/main/java/com/weedrice/whiteboard/domain/post/dto/PostSummary.java'
              - 'backend/src/main/java/com/weedrice/whiteboard/domain/post/dto/DraftResponse.java'
              - 'backend/src/main/java/com/weedrice/whiteboard/domain/feed/dto/FeedPostSummary.java'
              - 'backend/src/main/java/com/weedrice/whiteboard/domain/comment/dto/CommentResponse.java'
              - 'backend/src/main/java/com/weedrice/whiteboard/domain/board/dto/**'
              - 'backend/src/main/java/com/weedrice/whiteboard/domain/auth/dto/VerifyCodeResponse.java'
              - 'backend/src/main/java/com/weedrice/whiteboard/domain/user/dto/NotificationSettingResponse.java'
              # 생성 타입의 입력. 스펙이 바뀌면 api:check와 타입 검사가 돌아야 한다.
              - 'docs/api/openapi-frontend.json'
              - 'docker-compose.yml'
              - '.github/workflows/ci.yml'
              - '.github/workflows/deploy-frontend.yml'
            ops:
              - 'deploy/**'
              - '.github/CODEOWNERS'
              - 'docs/ops/api-contract-revision.txt'
              - 'docs/ops/postgres-backup-restore.md'
              - 'backend/scripts/**'
              - 'docker-compose*.yml'
              - 'backend/Dockerfile'
              - 'frontend/Dockerfile'
              - 'backend/gradle/wrapper/**'
              - '.github/workflows/**'
              - '.github/dependabot.yml'
              - 'docs/design-notes/**'
              - 'docs/ops/applied-contract-migrations.txt'
              - 'docs/ops/database-migration-policy.md'
              - 'docs/ops/**'

      - name: Resolve last successful deployment baselines
        id: deployment-baselines
        if: >-
          github.ref == 'refs/heads/main' &&
          (github.event_name == 'push' ||
          (github.event_name == 'workflow_dispatch' && inputs.deploy_backend))
        env:
          GH_TOKEN: ${{ github.token }}
          DEPLOYMENT_WORKFLOW: ci.yml
          DEPLOYMENT_BRANCH: main
        run: node deploy/scripts/resolve-deployment-baselines.mjs

      - name: Detect backend changes since the last successful deployment
        id: backend-deploy-filter
        if: >-
          github.event_name == 'push' &&
          github.ref == 'refs/heads/main' &&
          steps.deployment-baselines.outputs.backend_found == 'true'
        uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # v4
        with:
          base: ${{ steps.deployment-baselines.outputs.backend_sha }}
          ref: ${{ github.sha }}
          predicate-quantifier: 'every'
          filters: |
            backend:
              - 'backend/**'
              - '!backend/src/test/**'
              - '!backend/src/testFixtures/**'
              - '!backend/scripts/**'
              - '!backend/**/*.md'
              - '!backend/DATABASE_INIT.sql'
              - '!backend/Dockerfile'
              - '!backend/.dockerignore'
              - '!backend/.gitignore'
              - '!backend/compileTestJava.log'
            contract:
              - 'docs/ops/api-contract-revision.txt'

      - name: Detect frontend changes since the last successful deployment
        id: frontend-deploy-filter
        if: >-
          github.event_name == 'push' &&
          github.ref == 'refs/heads/main' &&
          steps.deployment-baselines.outputs.frontend_found == 'true'
        uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # v4
        with:
          base: ${{ steps.deployment-baselines.outputs.frontend_sha }}
          ref: ${{ github.sha }}
          predicate-quantifier: 'every'
          filters: |
            frontend:
              - 'frontend/**'
              - '!frontend/**/__tests__/**'
              - '!frontend/**/*.spec.*'
              - '!frontend/**/*.test.*'
              - '!frontend/e2e/**'
              - '!frontend/docs/**'
              - '!frontend/scripts/__tests__/**'
              - '!frontend/**/*.md'
              - '!frontend/.dockerignore'
              - '!frontend/.gitignore'
              - '!frontend/.vscode/**'
              - '!frontend/Dockerfile'
              - '!frontend/nginx.conf'
              - '!frontend/eslint.config.mjs'
              - '!frontend/jsconfig.json'
              - '!frontend/playwright*.config.ts'
              - '!frontend/vitest.config.ts'
            contract:
              - 'docs/ops/api-contract-revision.txt'

      - name: Resolve deployment scope
        id: deploy-scope
        env:
          IS_MAIN_PUSH: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
          BACKEND_BASE_FOUND: ${{ steps.deployment-baselines.outputs.backend_found }}
          FRONTEND_BASE_FOUND: ${{ steps.deployment-baselines.outputs.frontend_found }}
          BACKEND_DEPLOY_CHANGED: ${{ steps.backend-deploy-filter.outputs.backend }}
          BACKEND_CONTRACT_CHANGED: ${{ steps.backend-deploy-filter.outputs.contract }}
          FRONTEND_DEPLOY_CHANGED: ${{ steps.frontend-deploy-filter.outputs.frontend }}
          FRONTEND_CONTRACT_CHANGED: ${{ steps.frontend-deploy-filter.outputs.contract }}
        run: |
          backend=false
          frontend=false
          if [ "$IS_MAIN_PUSH" = "true" ]; then
            backend="${BACKEND_DEPLOY_CHANGED:-false}"
            frontend="${FRONTEND_DEPLOY_CHANGED:-false}"
            if [ "$BACKEND_BASE_FOUND" != "true" ] || [ "$BACKEND_CONTRACT_CHANGED" = "true" ]; then
              backend=true
            fi
            if [ "$FRONTEND_BASE_FOUND" != "true" ] || [ "$FRONTEND_CONTRACT_CHANGED" = "true" ]; then
              frontend=true
            fi
          fi
          {
            echo "backend=$backend"
            echo "frontend=$frontend"
          } >> "$GITHUB_OUTPUT"

      - name: Resolve validation scope
        id: scope
        env:
          BACKEND_CHANGED: ${{ steps.filter.outputs.backend }}
          FRONTEND_CHANGED: ${{ steps.filter.outputs.frontend }}
          OPS_CHANGED: ${{ steps.filter.outputs.ops }}
          BACKEND_DEPLOY_PENDING: ${{ steps.deploy-scope.outputs.backend }}
          FRONTEND_DEPLOY_PENDING: ${{ steps.deploy-scope.outputs.frontend }}
          MANUAL_BACKEND: ${{ github.event_name == 'workflow_dispatch' && inputs.deploy_backend || false }}
          MANUAL_FRONTEND: ${{ github.event_name == 'workflow_dispatch' && inputs.deploy_frontend || false }}
        run: |
          backend="$BACKEND_CHANGED"
          frontend="$FRONTEND_CHANGED"
          ops="$OPS_CHANGED"
          if [ "$BACKEND_DEPLOY_PENDING" = "true" ]; then backend=true; fi
          if [ "$FRONTEND_DEPLOY_PENDING" = "true" ]; then frontend=true; fi
          if [ "$MANUAL_BACKEND" = "true" ]; then backend=true; fi
          if [ "$MANUAL_FRONTEND" = "true" ]; then frontend=true; fi
          if [ "$MANUAL_BACKEND" = "true" ] || [ "$MANUAL_FRONTEND" = "true" ]; then ops=true; fi
          {
            echo "backend=$backend"
            echo "frontend=$frontend"
            echo "ops=$ops"
          } >> "$GITHUB_OUTPUT"

  backend-test:
    needs: changes
    if: needs.changes.outputs.backend == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: 25
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
        working-directory: backend

      - name: Run tests
        run: ./gradlew test jacocoTestCoverageVerification
        working-directory: backend

      - name: Upload test report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: backend-test-report
          path: backend/build/reports/tests/test
          retention-days: 30

      - name: Upload coverage report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: backend-coverage-report
          path: backend/build/reports/jacoco/html
          retention-days: 30

      - name: Build backend container image
        run: |
          docker build --build-arg SKIP_TESTS=true --build-arg BUILD_COMMIT_SHA="$GITHUB_SHA" -t noviis-backend:ci backend
          test "$(docker image inspect --format '{{.Config.User}}' noviis-backend:ci)" = app
          runtime_settings="$(docker run --rm --entrypoint java noviis-backend:ci -XshowSettings:properties -version 2>&1)"
          grep -q 'java.specification.version = 25' <<<"$runtime_settings"
        working-directory: .

  frontend-test:
    needs: changes
    if: needs.changes.outputs.frontend == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: frontend

      - name: Lint
        run: npm run lint:ci
        working-directory: frontend

      - name: Check i18n keys
        run: npm run check:i18n
        working-directory: frontend

      - name: Check UI conventions
        run: npm run check:ui
        working-directory: frontend

      # 커밋된 생성 타입이 스펙 스냅샷과 어긋나지 않았는지 본다.
      # 백엔드 쪽 짝은 OpenApiSpecSnapshotTest(스펙 스냅샷 <-> 실제 컨트롤러)다.
      - name: Check generated API types
        run: npm run api:check
        working-directory: frontend

      # generatedApiConformance.ts가 여기서 검사된다. 백엔드가 wire 이름을 바꾸면
      # 생성 타입이 바뀌고, 수기 타입과 어긋나 이 단계가 깨진다.
      - name: Type check
        run: npm run type-check
        working-directory: frontend

      - name: Run tests with coverage
        run: npm run coverage
        working-directory: frontend

      - name: Build
        run: npm run build
        working-directory: frontend

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium firefox webkit
        working-directory: frontend

      - name: Run sandboxed HTML browser tests
        run: npm run test:e2e:sandbox
        working-directory: frontend

      - name: Run Playwright E2E and accessibility tests
        run: npm run test:e2e:run
        working-directory: frontend

      - name: Run protected download cross-browser tests
        run: npm run test:e2e:download
        working-directory: frontend

      - name: Upload test coverage
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: frontend-coverage-report
          path: frontend/coverage
          if-no-files-found: ignore
          retention-days: 30

      - name: Upload Playwright artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: frontend-playwright-artifacts
          path: |
            frontend/playwright-report
            frontend/playwright-report-download
            frontend/test-results
          if-no-files-found: ignore
          retention-days: 30

      - name: Build frontend container image
        run: |
          set -euo pipefail
          docker build -t noviis-frontend:ci frontend
          test "$(docker image inspect --format '{{.Config.User}}' noviis-frontend:ci)" = nginx
          network="noviis-frontend-ci-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT"
          backend_container="noviis-frontend-backend-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT"
          cleanup() {
            docker rm -f "$backend_container" >/dev/null 2>&1 || true
            docker network rm "$network" >/dev/null 2>&1 || true
          }
          trap cleanup EXIT
          docker network create "$network" >/dev/null
          docker run -d --name "$backend_container" --network "$network" --network-alias backend \
            nginx:1.27.4-alpine@sha256:4ff102c5d78d254a6f0da062b3cf39eaf07f01eec0927fd21e219d0af8bc0591 >/dev/null
          docker run --rm --network "$network" --entrypoint nginx noviis-frontend:ci -t
        working-directory: .

  backend-postgres-migration:
    needs: changes
    if: needs.changes.outputs.backend == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    permissions:
      contents: read
    outputs:
      contract_migration: ${{ steps.migration_guard.outputs.contract_migration }}
    services:
      postgres:
        image: pgvector/pgvector:pg16@sha256:1d533553fefe4f12e5d80c7b80622ba0c382abb5758856f52983d8789179f0fb
        env:
          POSTGRES_DB: noviis_ci
          POSTGRES_USER: noviis_ci
          POSTGRES_PASSWORD: noviis_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U noviis_ci -d noviis_ci"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Resolve backend migration comparison base
        id: migration-base
        env:
          DEPLOYMENT_REQUESTED: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.changes.outputs.backend_deploy == 'true') || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && inputs.deploy_backend) }}
          DEPLOYMENT_BASE_FOUND: ${{ needs.changes.outputs.backend_deployment_base_found }}
          DEPLOYMENT_BASE_SHA: ${{ needs.changes.outputs.backend_deployment_base_sha }}
          EVENT_BASE_REF: ${{ github.event_name == 'pull_request' && format('origin/{0}', github.base_ref) || (github.event_name == 'push' && github.event.before || 'HEAD^') }}
        run: |
          set -euo pipefail
          base_ref="$EVENT_BASE_REF"
          force_contract=false
          if [ "$DEPLOYMENT_REQUESTED" = "true" ]; then
            if [ "$DEPLOYMENT_BASE_FOUND" = "true" ] && [ -n "$DEPLOYMENT_BASE_SHA" ]; then
              base_ref="$DEPLOYMENT_BASE_SHA"
            else
              force_contract=true
              echo "::warning::A successful backend deployment baseline was not found; explicit contract-migration approval will be required."
            fi
          fi
          {
            echo "base_ref=$base_ref"
            echo "force_contract=$force_contract"
          } >> "$GITHUB_OUTPUT"

      - name: Check migration compatibility
        id: migration_guard
        env:
          BASE_REF: ${{ steps.migration-base.outputs.base_ref }}
          FORCE_CONTRACT: ${{ steps.migration-base.outputs.force_contract }}
        run: |
          set -euo pipefail
          bash backend/scripts/check-migration-compatibility.sh "$BASE_REF" HEAD
          if [ "$FORCE_CONTRACT" = "true" ]; then
            echo "contract_migration=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Install PostgreSQL client
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      - name: Apply PostgreSQL migrations
        run: |
          set -euo pipefail
          for migration in $(find backend/src/main/resources/db/migration -name 'V*.sql' | sort -V); do
            echo "Applying ${migration}"
            if [ -f "${migration}.conf" ] && grep -Fqx 'executeInTransaction=false' "${migration}.conf"; then
              psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f "$migration"
            else
              psql "$DATABASE_URL" -v ON_ERROR_STOP=1 --single-transaction -f "$migration"
            fi
          done
        env:
          DATABASE_URL: postgresql://noviis_ci:noviis_ci@localhost:5432/noviis_ci

      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: 25
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
        working-directory: backend

      - name: Verify database documentation matches Flyway
        run: python3 backend/scripts/verify-database-doc.py

      - name: Create PostgreSQL app smoke database
        run: createdb -h localhost -U noviis_ci noviis_ci_app
        env:
          PGPASSWORD: noviis_ci

      - name: Run PostgreSQL application context smoke test
        run: ./gradlew test --tests "*PostgresApplicationContextSmokeTest" --rerun-tasks
        working-directory: backend
        env:
          POSTGRES_SMOKE_TEST: "true"
          POSTGRES_SMOKE_DATASOURCE_URL: jdbc:postgresql://localhost:5432/noviis_ci_app
          POSTGRES_SMOKE_DATASOURCE_USERNAME: noviis_ci
          POSTGRES_SMOKE_DATASOURCE_PASSWORD: noviis_ci
          FLYWAY_POSTGRESQL_TRANSACTIONAL_LOCK: "false"
          SPRING_FLYWAY_POSTGRESQL_TRANSACTIONAL_LOCK: "false"

      - name: Verify previous backend against the expanded schema
        env:
          BASE_REF: ${{ steps.migration-base.outputs.base_ref }}
          POSTGRES_SMOKE_TEST: "true"
          POSTGRES_SMOKE_DATASOURCE_URL: jdbc:postgresql://localhost:5432/noviis_ci
          POSTGRES_SMOKE_DATASOURCE_USERNAME: noviis_ci
          POSTGRES_SMOKE_DATASOURCE_PASSWORD: noviis_ci
          POSTGRES_EXPANDED_SCHEMA_TEST: "true"
          SPRING_FLYWAY_ENABLED: "false"
        run: |
          set -euo pipefail
          previous="$RUNNER_TEMP/previous-backend"
          git worktree add --detach "$previous" "$BASE_REF"
          trap 'git worktree remove --force "$previous"' EXIT
          cp backend/gradle/verification-metadata.xml \
            "$previous/backend/gradle/verification-metadata.xml"
          compatibility_test=backend/src/test/java/com/weedrice/whiteboard/PostgresExpandedSchemaCompatibilityTest.java
          cp "$compatibility_test" "$previous/$compatibility_test"
          chmod +x "$previous/backend/gradlew"
          "$previous/backend/gradlew" -p "$previous/backend" test \
            --tests '*PostgresExpandedSchemaCompatibilityTest' --rerun-tasks

  backend-postgres-upgrade:
    needs: changes
    if: needs.changes.outputs.backend == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    permissions:
      contents: read
    services:
      postgres:
        image: pgvector/pgvector:pg16@sha256:1d533553fefe4f12e5d80c7b80622ba0c382abb5758856f52983d8789179f0fb
        env:
          POSTGRES_DB: noviis_ci_upgrade
          POSTGRES_USER: noviis_ci
          POSTGRES_PASSWORD: noviis_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U noviis_ci -d noviis_ci_upgrade"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - name: Resolve backend upgrade comparison base
        id: upgrade-base
        env:
          DEPLOYMENT_REQUESTED: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.changes.outputs.backend_deploy == 'true') || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && inputs.deploy_backend) }}
          DEPLOYMENT_BASE_FOUND: ${{ needs.changes.outputs.backend_deployment_base_found }}
          DEPLOYMENT_BASE_SHA: ${{ needs.changes.outputs.backend_deployment_base_sha }}
          EVENT_BASE_REF: ${{ github.event_name == 'pull_request' && format('origin/{0}', github.base_ref) || (github.event_name == 'push' && github.event.before || 'HEAD^') }}
        run: |
          set -euo pipefail
          base_ref="$EVENT_BASE_REF"
          if [ "$DEPLOYMENT_REQUESTED" = "true" ] &&
             [ "$DEPLOYMENT_BASE_FOUND" = "true" ] && [ -n "$DEPLOYMENT_BASE_SHA" ]; then
            base_ref="$DEPLOYMENT_BASE_SHA"
          fi
          echo "base_ref=$base_ref" >> "$GITHUB_OUTPUT"
      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: 25
          cache: 'gradle'
      - name: Install PostgreSQL client
        run: sudo apt-get update && sudo apt-get install -y postgresql-client
      - name: Verify Flyway upgrade from the previous revision
        env:
          BASE_REF: ${{ steps.upgrade-base.outputs.base_ref }}
          PGPASSWORD: noviis_ci
          FLYWAY_POSTGRESQL_TRANSACTIONAL_LOCK: "false"
          SPRING_FLYWAY_POSTGRESQL_TRANSACTIONAL_LOCK: "false"
        run: |
          set -euo pipefail
          previous="$RUNNER_TEMP/previous-backend-upgrade"
          git worktree add --detach "$previous" "$BASE_REF"
          trap 'git worktree remove --force "$previous"' EXIT
          cp backend/gradle/verification-metadata.xml \
            "$previous/backend/gradle/verification-metadata.xml"
          chmod +x "$previous/backend/gradlew" ./backend/gradlew
          POSTGRES_SMOKE_TEST=true \
          POSTGRES_SMOKE_DATASOURCE_URL="jdbc:postgresql://localhost:5432/noviis_ci_upgrade" \
          POSTGRES_SMOKE_DATASOURCE_USERNAME=noviis_ci \
          POSTGRES_SMOKE_DATASOURCE_PASSWORD=noviis_ci \
            "$previous/backend/gradlew" -p "$previous/backend" test \
              --tests '*PostgresApplicationContextSmokeTest.contextLoadsWithPostgresMigrations' --rerun-tasks
          "$previous/backend/gradlew" -p "$previous/backend" --stop
          # The base revision may still have schedulers enabled. Drain any lingering
          # sessions before the current revision applies concurrent-index migrations.
          remaining_sessions=1
          for _ in {1..10}; do
            psql -h localhost -U noviis_ci -d postgres -Atc \
              "select pg_terminate_backend(pid) from pg_stat_activity where datname = 'noviis_ci_upgrade' and pid <> pg_backend_pid()" >/dev/null
            remaining_sessions="$(psql -h localhost -U noviis_ci -d postgres -Atc \
              "select count(*) from pg_stat_activity where datname = 'noviis_ci_upgrade' and pid <> pg_backend_pid()")"
            if [ "$remaining_sessions" = 0 ]; then
              break
            fi
            sleep 1
          done
          test "$remaining_sessions" = 0
          POSTGRES_SMOKE_TEST=true \
          POSTGRES_SMOKE_DATASOURCE_URL="jdbc:postgresql://localhost:5432/noviis_ci_upgrade" \
          POSTGRES_SMOKE_DATASOURCE_USERNAME=noviis_ci \
          POSTGRES_SMOKE_DATASOURCE_PASSWORD=noviis_ci \
            ./backend/gradlew -p backend test \
              --tests '*PostgresApplicationContextSmokeTest.contextLoadsWithPostgresMigrations' --rerun-tasks
          while IFS= read -r migration; do
            version="${migration##*/V}"
            version="${version%%__*}"
            test "$(psql -h localhost -U noviis_ci -d noviis_ci_upgrade -Atc \
              "select count(*) from flyway_schema_history where version = '$version' and success")" = 1
          done < <(find backend/src/main/resources/db/migration -type f -name 'V*.sql' | sort -V)

  ops-config-test:
    needs: changes
    if: needs.changes.outputs.ops == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Validate GitHub Actions workflows
        run: >-
          docker run --rm -v "$PWD:/repo" -w /repo
          rhysd/actionlint:1.7.7@sha256:887a259a5a534f3c4f36cb02dca341673c6089431057242cdc931e9f133147e9
          -ignore 'unexpected key.*queue.*concurrency'
          -ignore 'artifact-metadata'

      - name: Install workflow AST validator
        run: npm ci --ignore-scripts
        working-directory: frontend

      - name: Validate Prometheus configuration and rules
        run: |
          source deploy/monitoring/tool-versions.env
          dashboard_rules="$RUNNER_TEMP/noviis-dashboard-promql.json"
          python3 deploy/monitoring/verify-grafana-dashboard.py --promql-output "$dashboard_rules"
          bash deploy/monitoring/tests/verify-grafana-dashboard-test.sh
          docker run --rm --entrypoint /bin/promtool \
            -v "$PWD/deploy/monitoring/prometheus:/etc/prometheus:ro" \
            "$PROMETHEUS_VALIDATION_IMAGE" \
            check config /etc/prometheus/prometheus.yml
          docker run --rm --entrypoint /bin/promtool \
            -v "$PWD/deploy/monitoring/prometheus:/etc/prometheus:ro" \
            "$PROMETHEUS_VALIDATION_IMAGE" \
            check rules /etc/prometheus/noviis-alerts.yml
          docker run --rm --entrypoint /bin/promtool -w /etc/prometheus \
            -v "$PWD/deploy/monitoring/prometheus:/etc/prometheus:ro" \
            "$PROMETHEUS_VALIDATION_IMAGE" \
            test rules noviis-alerts.test.yml
          docker run --rm --entrypoint /bin/promtool \
            -v "$dashboard_rules:/tmp/noviis-dashboard-promql.json:ro" \
            "$PROMETHEUS_VALIDATION_IMAGE" \
            check rules /tmp/noviis-dashboard-promql.json

      - name: Verify scheduled job freshness manifest
        run: |
          python3 deploy/monitoring/verify-scheduled-jobs.py
          python3 deploy/monitoring/verify-required-backend-metrics.py
          bash deploy/monitoring/verify-resource-limit-evidence.sh

      - name: Validate Grafana version manifest and dashboard JSON
        run: |
          source deploy/monitoring/tool-versions.env
          [[ "$NODE_EXPORTER_MIN_VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]
          archive="$RUNNER_TEMP/grafana-${GRAFANA_VERSION}.${GRAFANA_ARCH}.tar.gz"
          extract_root="$RUNNER_TEMP/grafana-validation"
          curl --fail --location --retry 3 --proto '=https' \
            --output "$archive" \
            "https://dl.grafana.com/oss/release/grafana-${GRAFANA_VERSION}.${GRAFANA_ARCH}.tar.gz"
          bash deploy/monitoring/verify-grafana-archive.sh \
            "$archive" "$GRAFANA_ARCHIVE_SHA256" "$GRAFANA_ARCH"
          mkdir -p "$extract_root"
          tar -xzf "$archive" -C "$extract_root"
          grafana_binary="$(find "$extract_root" -type f -path '*/bin/grafana' -print -quit)"
          test -n "$grafana_binary"
          grafana_version_output="$("$grafana_binary" --version)"
          printf '%s\n' "$grafana_version_output"
          grep -F -- "$GRAFANA_VERSION" <<< "$grafana_version_output"
          jq -e . deploy/monitoring/grafana/dashboards/noviis-overview.json >/dev/null

      - name: Validate shell scripts
        run: |
          find deploy -type f -name '*.sh' -print0 | xargs -0 -r -n1 bash -n
          find backend/scripts -type f -name '*.sh' -print0 | xargs -0 -r -n1 bash -n
          find deploy -type f -name '*.sh' -print0 | xargs -0 -r shellcheck
          find backend/scripts -type f -name '*.sh' -print0 | xargs -0 -r shellcheck

      - name: Validate sudoers policy and command matrix
        run: |
          sudo visudo -cf deploy/sudoers/noviis-deploy
          sudo bash deploy/sudoers/tests/noviis-deploy-test.sh

      - name: Run deployment script fixtures
        run: |
          bash deploy/scripts/tests/activate-backend-release-test.sh
          bash deploy/scripts/tests/verify-backend-start-state-test.sh
          bash deploy/scripts/tests/activate-frontend-release-test.sh
          bash deploy/scripts/tests/verify-active-frontend-release-test.sh
          bash deploy/scripts/tests/verify-release-provenance-test.sh
          bash deploy/scripts/tests/verify-deployment-freshness-test.sh
          bash deploy/scripts/tests/download-attestation-with-retry-test.sh
          bash deploy/scripts/tests/record-cleanup-debt-test.sh
          node deploy/scripts/tests/resolve-deployment-baselines-test.mjs
          bash deploy/scripts/tests/verify-workflow-security-test.sh
          bash backend/scripts/tests/check-migration-compatibility-test.sh
          sudo bash deploy/monitoring/tests/rotate-grafana-admin-password-test.sh
          bash deploy/monitoring/tests/verify-grafana-archive-test.sh
          bash deploy/monitoring/tests/check-prometheus-health-test.sh
          bash deploy/monitoring/tests/verify-resource-limit-evidence-test.sh

      - name: Validate repository migration policy changes
        env:
          BASE_REF: ${{ github.event_name == 'pull_request' && format('origin/{0}', github.base_ref) || (github.event_name == 'push' && github.event.before || 'HEAD^') }}
        run: bash backend/scripts/check-migration-compatibility.sh "$BASE_REF" HEAD

      - name: Validate production Nginx configuration
        run: bash deploy/scripts/test-nginx-config.sh

      - name: Validate systemd units and drop-ins
        run: |
          python3 deploy/systemd/verify-hardening.py
          bash deploy/systemd/tests/verify-hardening-test.sh
          systemd-analyze verify deploy/systemd/app.service
          grep -Fqx 'OOMPolicy=stop' deploy/systemd/app.service
          grep -Fqx 'StartLimitIntervalSec=300' deploy/systemd/app.service
          grep -Fqx 'StartLimitBurst=5' deploy/systemd/app.service
          grep -Fqx 'ExecStartPre=+/usr/local/sbin/verify-noviis-backend-start-state' deploy/systemd/app.service
          tmp="$(mktemp -d)"
          trap 'rm -rf "$tmp"' EXIT
          for service in prometheus grafana-server; do
            mkdir -p "$tmp/${service}.service.d"
            printf '[Unit]\nDescription=CI fixture\n[Service]\nExecStart=/bin/true\n' > "$tmp/${service}.service"
            cp "deploy/monitoring/systemd/${service}.service.d/override.conf" "$tmp/${service}.service.d/override.conf"
            if [ "$service" = prometheus ]; then
              sed -i 's#/usr/bin/prometheus#/bin/true#' "$tmp/${service}.service.d/override.conf"
            fi
            SYSTEMD_UNIT_PATH="$tmp:" systemd-analyze verify "${service}.service"
          done
          mkdir -p "$tmp/prometheus-node-exporter.service.d"
          printf '[Unit]\nDescription=CI fixture\n[Service]\nExecStart=/bin/true\n' > "$tmp/prometheus-node-exporter.service"
          grep -Fqx 'ExecStart=/usr/bin/prometheus-node-exporter --web.listen-address=127.0.0.1:9100 --collector.textfile.directory=/var/lib/prometheus/node-exporter' \
            deploy/monitoring/systemd/prometheus-node-exporter.service.d/override.conf
          sed 's#/usr/bin/prometheus-node-exporter#/bin/true#' \
            deploy/monitoring/systemd/prometheus-node-exporter.service.d/override.conf \
            > "$tmp/prometheus-node-exporter.service.d/override.conf"
          SYSTEMD_UNIT_PATH="$tmp:" systemd-analyze verify prometheus-node-exporter.service
          sed 's#/usr/local/sbin/check-noviis-prometheus-health#/bin/true#' \
            deploy/monitoring/systemd/noviis-prometheus-watchdog.service \
            > "$tmp/noviis-prometheus-watchdog.service"
          cp deploy/monitoring/systemd/noviis-prometheus-watchdog.timer "$tmp/noviis-prometheus-watchdog.timer"
          SYSTEMD_UNIT_PATH="$tmp:" systemd-analyze verify noviis-prometheus-watchdog.service noviis-prometheus-watchdog.timer

      - name: Validate Compose configuration
        env:
          JWT_SECRET_DEV: Y2ktb25seS1kdW1teS1qd3Qtc2VjcmV0LWtleS0zMi1ieXRlcw==
        run: docker compose config --quiet

  ci-gate:
    name: CI Gate
    if: always()
    needs:
      - changes
      - backend-test
      - frontend-test
      - backend-postgres-migration
      - backend-postgres-upgrade
      - ops-config-test
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      contract_migration: ${{ steps.verify.outputs.contract_migration }}
    steps:
      - name: Verify required jobs
        id: verify
        env:
          CHANGES_RESULT: ${{ needs.changes.result }}
          BACKEND_RESULT: ${{ needs.backend-test.result }}
          FRONTEND_RESULT: ${{ needs.frontend-test.result }}
          POSTGRES_RESULT: ${{ needs.backend-postgres-migration.result }}
          POSTGRES_UPGRADE_RESULT: ${{ needs.backend-postgres-upgrade.result }}
          OPS_RESULT: ${{ needs.ops-config-test.result }}
          NEED_BACKEND: ${{ needs.changes.outputs.backend }}
          NEED_FRONTEND: ${{ needs.changes.outputs.frontend }}
          NEED_OPS: ${{ needs.changes.outputs.ops }}
          CONTRACT_MIGRATION: ${{ needs.backend-postgres-migration.outputs.contract_migration }}
        run: |
          if [ "$CHANGES_RESULT" != "success" ]; then
            echo "Change detection failed: $CHANGES_RESULT"
            exit 1
          fi

          verify_job() {
            local required="$1"
            local result="$2"
            local name="$3"
            if [ "$required" = "true" ] && [ "$result" != "success" ]; then
              echo "$name was required but finished as $result"
              exit 1
            fi
            if [ "$required" != "true" ] && [ "$result" != "skipped" ]; then
              echo "$name was not selected but finished as $result"
              exit 1
            fi
          }
          verify_job "$NEED_BACKEND" "$BACKEND_RESULT" backend-test
          verify_job "$NEED_BACKEND" "$POSTGRES_RESULT" backend-postgres-migration
          verify_job "$NEED_BACKEND" "$POSTGRES_UPGRADE_RESULT" backend-postgres-upgrade
          verify_job "$NEED_FRONTEND" "$FRONTEND_RESULT" frontend-test
          verify_job "$NEED_OPS" "$OPS_RESULT" ops-config-test
          if [ "$CONTRACT_MIGRATION" != "true" ]; then CONTRACT_MIGRATION=false; fi
          echo "contract_migration=$CONTRACT_MIGRATION" >> "$GITHUB_OUTPUT"

  candidate-backend:
    needs: [changes, ci-gate]
    if: >-
      !cancelled() &&
      needs.ci-gate.result == 'success' &&
      ((github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.changes.outputs.backend_deploy == 'true') ||
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && inputs.deploy_backend))
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: read
    outputs:
      artifact_id: ${{ steps.upload-candidate.outputs.artifact-id }}
      producer_attempt: ${{ steps.artifact-identity.outputs.producer_attempt }}
    steps:
      - name: Record backend candidate identity
        id: artifact-identity
        run: echo "producer_attempt=$GITHUB_RUN_ATTEMPT" >> "$GITHUB_OUTPUT"
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: 25
          cache: gradle
      - name: Build backend release once after the CI gate
        working-directory: backend
        env:
          BUILD_COMMIT_SHA: ${{ github.sha }}
          CONTRACT_MIGRATION: ${{ needs.ci-gate.outputs.contract_migration }}
        run: |
          set -euo pipefail
          chmod +x gradlew
          ./gradlew bootJar --no-daemon
          install -d -m 0755 release
          install -m 0644 build/libs/app.jar release/app.jar
          api_contract_revision="$(tr -d '\r\n' < ../docs/ops/api-contract-revision.txt)"
          [[ "$api_contract_revision" =~ ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$ ]]
          case "$CONTRACT_MIGRATION" in true|false) ;; *) exit 1 ;; esac
          {
            printf 'repository=%s\ncommit_sha=%s\nrun_id=%s\nrun_number=%s\nrun_attempt=%s\napi_contract_revision=%s\n' \
              "$GITHUB_REPOSITORY" "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_NUMBER" "$GITHUB_RUN_ATTEMPT" "$api_contract_revision"
            printf 'contract_migration=%s\n' "$CONTRACT_MIGRATION"
          } > release/RELEASE_METADATA
      - name: Generate backend SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          path: backend/release
          format: spdx-json
          output-file: backend/release/sbom.spdx.json
          upload-artifact: false
      - name: Finalize backend checksum manifest
        run: |
          set -euo pipefail
          sha256sum app.jar RELEASE_METADATA sbom.spdx.json > SHA256SUMS
          printf 'release_type=backend\ncommit_sha=%s\n' "$GITHUB_SHA" > RELEASE_ENVELOPE
          sha256sum app.jar RELEASE_METADATA sbom.spdx.json SHA256SUMS >> RELEASE_ENVELOPE
        working-directory: backend/release
      - name: Upload untrusted backend candidate
        id: upload-candidate
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: backend-candidate-${{ github.run_id }}-${{ github.run_attempt }}-${{ github.sha }}
          path: backend/release
          if-no-files-found: error
          retention-days: 1

  release-backend:
    needs: [candidate-backend, ci-gate]
    if: >-
      !cancelled() &&
      needs.candidate-backend.result == 'success' &&
      needs.ci-gate.result == 'success'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
      id-token: write
      attestations: write
      artifact-metadata: write
    outputs:
      artifact_id: ${{ steps.upload-release.outputs.artifact-id }}
      metadata_attempt: ${{ steps.release-identity.outputs.metadata_attempt }}
    steps:
      - name: Checkout trusted attestation helper
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
          sparse-checkout: deploy/scripts/download-attestation-with-retry.sh
          sparse-checkout-cone-mode: false
      - name: Validate backend candidate identity
        env:
          CANDIDATE_ARTIFACT_ID: ${{ needs.candidate-backend.outputs.artifact_id }}
          CANDIDATE_ATTEMPT: ${{ needs.candidate-backend.outputs.producer_attempt }}
        run: |
          [[ "$CANDIDATE_ARTIFACT_ID" =~ ^[1-9][0-9]*$ ]]
          [[ "$CANDIDATE_ATTEMPT" =~ ^[1-9][0-9]*$ ]]
      - name: Download backend candidate
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          artifact-ids: ${{ needs.candidate-backend.outputs.artifact_id }}
          path: backend/release
      - name: Verify candidate digest and immutable metadata
        working-directory: backend/release
        env:
          CANDIDATE_ATTEMPT: ${{ needs.candidate-backend.outputs.producer_attempt }}
        run: |
          set -euo pipefail
          sha256sum --strict --check SHA256SUMS
          grep -Fqx -- "repository=$GITHUB_REPOSITORY" RELEASE_METADATA
          grep -Fqx -- "commit_sha=$GITHUB_SHA" RELEASE_METADATA
          grep -Fqx -- "run_id=$GITHUB_RUN_ID" RELEASE_METADATA
          grep -Fqx -- "run_number=$GITHUB_RUN_NUMBER" RELEASE_METADATA
          grep -Fqx -- "run_attempt=$CANDIDATE_ATTEMPT" RELEASE_METADATA
      - name: Attest backend release
        uses: actions/attest@508db95dd578ae2727ebd6217d5ba78e4fbda05d # v4.2.1
        with:
          subject-path: backend/release/RELEASE_ENVELOPE
      - name: Attest backend SBOM
        uses: actions/attest@508db95dd578ae2727ebd6217d5ba78e4fbda05d # v4.2.1
        with:
          subject-path: backend/release/app.jar
          sbom-path: backend/release/sbom.spdx.json
      - name: Download backend attestation bundle for server verification
        working-directory: backend/release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir envelope-attestation payload-attestation
          bash ../../deploy/scripts/download-attestation-with-retry.sh RELEASE_ENVELOPE "$GITHUB_REPOSITORY" envelope-attestation
          bash ../../deploy/scripts/download-attestation-with-retry.sh app.jar "$GITHUB_REPOSITORY" payload-attestation
          mapfile -t envelope_bundles < <(find envelope-attestation -maxdepth 1 -type f -name 'sha256*.jsonl' -print)
          mapfile -t payload_bundles < <(find payload-attestation -maxdepth 1 -type f -name 'sha256*.jsonl' -print)
          test "${#envelope_bundles[@]}" -eq 1
          test "${#payload_bundles[@]}" -eq 1
          cat -- "${envelope_bundles[0]}" "${payload_bundles[0]}" > PROVENANCE_BUNDLE.jsonl
          rm -rf -- envelope-attestation payload-attestation
      - name: Upload backend release
        id: upload-release
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: backend-release-${{ github.run_id }}-${{ github.run_attempt }}-${{ github.sha }}
          path: backend/release
          if-no-files-found: error
          retention-days: 14
      - name: Record backend release identity
        id: release-identity
        env:
          METADATA_ATTEMPT: ${{ needs.candidate-backend.outputs.producer_attempt }}
        run: echo "metadata_attempt=$METADATA_ATTEMPT" >> "$GITHUB_OUTPUT"

  candidate-frontend:
    needs: [changes, ci-gate]
    if: >-
      !cancelled() &&
      needs.ci-gate.result == 'success' &&
      ((github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.changes.outputs.frontend_deploy == 'true') ||
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && inputs.deploy_frontend))
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    permissions:
      contents: read
    outputs:
      artifact_id: ${{ steps.upload-candidate.outputs.artifact-id }}
      producer_attempt: ${{ steps.artifact-identity.outputs.producer_attempt }}
    steps:
      - name: Record frontend candidate identity
        id: artifact-identity
        run: echo "producer_attempt=$GITHUB_RUN_ATTEMPT" >> "$GITHUB_OUTPUT"
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Build frontend release once after the CI gate
        working-directory: frontend
        env:
          VITE_COMMIT_HASH: ${{ github.sha }}
          SEO_STRICT: "true"
          SEO_RELEASE_SHA: ${{ github.sha }}
          SITEMAP_SITE_URL: https://noviis.kr
          SITEMAP_API_BASE_URL: https://noviis.kr/api/v1
          PRERENDER_FETCH_RETRIES: 5
          PRERENDER_FETCH_RETRY_DELAY_MS: 2000
        run: |
          set -euo pipefail
          npm ci
          npm run build:seo
          api_contract_revision="$(tr -d '\r\n' < ../docs/ops/api-contract-revision.txt)"
          [[ "$api_contract_revision" =~ ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$ ]]
          printf '%s\n' "$GITHUB_SHA" > dist/.noviis-release
          printf '%s\n' "$api_contract_revision" > dist/.noviis-api-contract
          npm run seo:verify:dist
          tar -C dist -czf frontend-release.tar.gz .
          printf 'repository=%s\ncommit_sha=%s\nrun_id=%s\nrun_number=%s\nrun_attempt=%s\napi_contract_revision=%s\n' \
            "$GITHUB_REPOSITORY" "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_NUMBER" "$GITHUB_RUN_ATTEMPT" "$api_contract_revision" > RELEASE_METADATA
      - name: Generate frontend SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          path: frontend/dist
          format: spdx-json
          output-file: frontend/sbom.spdx.json
          upload-artifact: false
      - name: Finalize frontend checksum manifest
        run: |
          set -euo pipefail
          sha256sum frontend-release.tar.gz RELEASE_METADATA sbom.spdx.json > SHA256SUMS
          printf 'release_type=frontend\ncommit_sha=%s\n' "$GITHUB_SHA" > RELEASE_ENVELOPE
          sha256sum frontend-release.tar.gz RELEASE_METADATA sbom.spdx.json SHA256SUMS >> RELEASE_ENVELOPE
        working-directory: frontend
      - name: Upload untrusted frontend candidate
        id: upload-candidate
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: frontend-candidate-${{ github.run_id }}-${{ github.run_attempt }}-${{ github.sha }}
          path: |
            frontend/frontend-release.tar.gz
            frontend/RELEASE_METADATA
            frontend/SHA256SUMS
            frontend/sbom.spdx.json
            frontend/RELEASE_ENVELOPE
          if-no-files-found: error
          retention-days: 1

  release-frontend:
    needs: [candidate-frontend, ci-gate]
    if: >-
      !cancelled() &&
      needs.candidate-frontend.result == 'success' &&
      needs.ci-gate.result == 'success'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
      id-token: write
      attestations: write
      artifact-metadata: write
    outputs:
      artifact_id: ${{ steps.upload-release.outputs.artifact-id }}
      metadata_attempt: ${{ steps.release-identity.outputs.metadata_attempt }}
    steps:
      - name: Checkout trusted attestation helper
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
          sparse-checkout: deploy/scripts/download-attestation-with-retry.sh
          sparse-checkout-cone-mode: false
      - name: Validate frontend candidate identity
        env:
          CANDIDATE_ARTIFACT_ID: ${{ needs.candidate-frontend.outputs.artifact_id }}
          CANDIDATE_ATTEMPT: ${{ needs.candidate-frontend.outputs.producer_attempt }}
        run: |
          [[ "$CANDIDATE_ARTIFACT_ID" =~ ^[1-9][0-9]*$ ]]
          [[ "$CANDIDATE_ATTEMPT" =~ ^[1-9][0-9]*$ ]]
      - name: Download frontend candidate
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          artifact-ids: ${{ needs.candidate-frontend.outputs.artifact_id }}
          path: frontend
      - name: Verify candidate digest and immutable metadata
        working-directory: frontend
        env:
          CANDIDATE_ATTEMPT: ${{ needs.candidate-frontend.outputs.producer_attempt }}
        run: |
          set -euo pipefail
          sha256sum --strict --check SHA256SUMS
          grep -Fqx -- "repository=$GITHUB_REPOSITORY" RELEASE_METADATA
          grep -Fqx -- "commit_sha=$GITHUB_SHA" RELEASE_METADATA
          grep -Fqx -- "run_id=$GITHUB_RUN_ID" RELEASE_METADATA
          grep -Fqx -- "run_number=$GITHUB_RUN_NUMBER" RELEASE_METADATA
          grep -Fqx -- "run_attempt=$CANDIDATE_ATTEMPT" RELEASE_METADATA
      - name: Attest frontend release
        uses: actions/attest@508db95dd578ae2727ebd6217d5ba78e4fbda05d # v4.2.1
        with:
          subject-path: frontend/RELEASE_ENVELOPE
      - name: Attest frontend SBOM
        uses: actions/attest@508db95dd578ae2727ebd6217d5ba78e4fbda05d # v4.2.1
        with:
          subject-path: frontend/frontend-release.tar.gz
          sbom-path: frontend/sbom.spdx.json
      - name: Download frontend attestation bundle for server verification
        working-directory: frontend
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir envelope-attestation payload-attestation
          bash ../deploy/scripts/download-attestation-with-retry.sh RELEASE_ENVELOPE "$GITHUB_REPOSITORY" envelope-attestation
          bash ../deploy/scripts/download-attestation-with-retry.sh frontend-release.tar.gz "$GITHUB_REPOSITORY" payload-attestation
          mapfile -t envelope_bundles < <(find envelope-attestation -maxdepth 1 -type f -name 'sha256*.jsonl' -print)
          mapfile -t payload_bundles < <(find payload-attestation -maxdepth 1 -type f -name 'sha256*.jsonl' -print)
          test "${#envelope_bundles[@]}" -eq 1
          test "${#payload_bundles[@]}" -eq 1
          cat -- "${envelope_bundles[0]}" "${payload_bundles[0]}" > PROVENANCE_BUNDLE.jsonl
          rm -rf -- envelope-attestation payload-attestation
      - name: Upload frontend release
        id: upload-release
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: frontend-release-${{ github.run_id }}-${{ github.run_attempt }}-${{ github.sha }}
          path: |
            frontend/frontend-release.tar.gz
            frontend/RELEASE_METADATA
            frontend/SHA256SUMS
            frontend/sbom.spdx.json
            frontend/RELEASE_ENVELOPE
            frontend/PROVENANCE_BUNDLE.jsonl
          if-no-files-found: error
          retention-days: 14
      - name: Record frontend release identity
        id: release-identity
        env:
          METADATA_ATTEMPT: ${{ needs.candidate-frontend.outputs.producer_attempt }}
        run: echo "metadata_attempt=$METADATA_ATTEMPT" >> "$GITHUB_OUTPUT"

  deploy-backend:
    needs: [changes, ci-gate, release-backend]
    if: >-
      !cancelled() &&
      needs.ci-gate.result == 'success' &&
      needs.release-backend.result == 'success' &&
      ((github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.changes.outputs.backend_deploy == 'true') ||
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && inputs.deploy_backend)) &&
      (needs.ci-gate.outputs.contract_migration != 'true' ||
      (github.event_name == 'workflow_dispatch' && inputs.allow_contract_migration))
    uses: ./.github/workflows/deploy-backend.yml
    permissions:
      contents: read
      attestations: read
    with:
      expected_sha: ${{ github.sha }}
      release_artifact_id: ${{ needs.release-backend.outputs.artifact_id }}
      release_metadata_attempt: ${{ needs.release-backend.outputs.metadata_attempt }}
      contract_migration: ${{ needs.ci-gate.outputs.contract_migration == 'true' }}
    secrets:
      EC2_HOST: ${{ secrets.EC2_HOST }}
      EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
      EC2_USER: ${{ secrets.EC2_USER }}

  frontend-deploy-readiness:
    needs: [changes, ci-gate, release-frontend, deploy-backend]
    if: always() && !cancelled()
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      deploy: ${{ steps.readiness.outputs.deploy }}
      backend_activated_sha: ${{ steps.readiness.outputs.backend_activated_sha }}
      release_artifact_id: ${{ steps.readiness.outputs.release_artifact_id }}
      release_metadata_attempt: ${{ steps.readiness.outputs.release_metadata_attempt }}
    steps:
      - name: Resolve frontend deployment readiness
        id: readiness
        env:
          EVENT_NAME: ${{ github.event_name }}
          REF_NAME: ${{ github.ref }}
          CI_GATE_RESULT: ${{ needs.ci-gate.result }}
          RELEASE_FRONTEND_RESULT: ${{ needs.release-frontend.result }}
          DEPLOY_BACKEND_RESULT: ${{ needs.deploy-backend.result }}
          BACKEND_DEPLOY_CHANGED: ${{ needs.changes.outputs.backend_deploy }}
          FRONTEND_DEPLOY_CHANGED: ${{ needs.changes.outputs.frontend_deploy }}
          MANUAL_BACKEND: ${{ github.event_name == 'workflow_dispatch' && inputs.deploy_backend || false }}
          MANUAL_FRONTEND: ${{ github.event_name == 'workflow_dispatch' && inputs.deploy_frontend || false }}
          BACKEND_ACTIVATED_SHA: ${{ needs.deploy-backend.outputs.activated_sha || '' }}
          RELEASE_ARTIFACT_ID: ${{ needs.release-frontend.outputs.artifact_id }}
          RELEASE_METADATA_ATTEMPT: ${{ needs.release-frontend.outputs.metadata_attempt }}
        run: |
          set -euo pipefail
          frontend_requested=false
          backend_ready=false

          if [ "$EVENT_NAME" = "push" ] && [ "$REF_NAME" = "refs/heads/main" ] &&
             [ "$FRONTEND_DEPLOY_CHANGED" = "true" ]; then
            frontend_requested=true
          elif [ "$EVENT_NAME" = "workflow_dispatch" ] && [ "$REF_NAME" = "refs/heads/main" ] &&
               [ "$MANUAL_FRONTEND" = "true" ]; then
            frontend_requested=true
          fi

          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            if [ "$MANUAL_BACKEND" != "true" ] || [ "$DEPLOY_BACKEND_RESULT" = "success" ]; then
              backend_ready=true
            fi
          elif [ "$BACKEND_DEPLOY_CHANGED" != "true" ] || [ "$DEPLOY_BACKEND_RESULT" = "success" ]; then
            backend_ready=true
          fi

          deploy=false
          if [ "$CI_GATE_RESULT" = "success" ] &&
             [ "$RELEASE_FRONTEND_RESULT" = "success" ] &&
             [ "$frontend_requested" = "true" ] &&
             [ "$backend_ready" = "true" ]; then
            deploy=true
          fi

          {
            echo "deploy=$deploy"
            echo "backend_activated_sha=$BACKEND_ACTIVATED_SHA"
            echo "release_artifact_id=$RELEASE_ARTIFACT_ID"
            echo "release_metadata_attempt=$RELEASE_METADATA_ATTEMPT"
          } >> "$GITHUB_OUTPUT"
          echo "frontend_requested=$frontend_requested backend_ready=$backend_ready deploy=$deploy"

  deploy-frontend:
    needs: frontend-deploy-readiness
    if: >-
      !cancelled() &&
      needs.frontend-deploy-readiness.outputs.deploy == 'true'
    uses: ./.github/workflows/deploy-frontend.yml
    permissions:
      contents: read
      attestations: read
    with:
      expected_sha: ${{ github.sha }}
      release_artifact_id: ${{ needs.frontend-deploy-readiness.outputs.release_artifact_id }}
      release_metadata_attempt: ${{ needs.frontend-deploy-readiness.outputs.release_metadata_attempt }}
      backend_activated_sha: ${{ needs.frontend-deploy-readiness.outputs.backend_activated_sha }}
    secrets:
      EC2_HOST: ${{ secrets.EC2_HOST }}
      EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
      EC2_USER: ${{ secrets.EC2_USER }}

  deployment-gate:
    name: Deployment Gate
    needs:
      - changes
      - ci-gate
      - candidate-backend
      - release-backend
      - deploy-backend
      - candidate-frontend
      - release-frontend
      - frontend-deploy-readiness
      - deploy-frontend
    if: always() && !cancelled()
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Verify requested deployments completed
        env:
          EVENT_NAME: ${{ github.event_name }}
          REF_NAME: ${{ github.ref }}
          CI_GATE_RESULT: ${{ needs.ci-gate.result }}
          BACKEND_DEPLOY_CHANGED: ${{ needs.changes.outputs.backend_deploy }}
          FRONTEND_DEPLOY_CHANGED: ${{ needs.changes.outputs.frontend_deploy }}
          MANUAL_BACKEND: ${{ github.event_name == 'workflow_dispatch' && inputs.deploy_backend || false }}
          MANUAL_FRONTEND: ${{ github.event_name == 'workflow_dispatch' && inputs.deploy_frontend || false }}
          ALLOW_CONTRACT_MIGRATION: ${{ github.event_name == 'workflow_dispatch' && inputs.allow_contract_migration || false }}
          CONTRACT_MIGRATION: ${{ needs.ci-gate.outputs.contract_migration }}
          BACKEND_CANDIDATE_RESULT: ${{ needs.candidate-backend.result }}
          BACKEND_RELEASE_RESULT: ${{ needs.release-backend.result }}
          BACKEND_DEPLOY_RESULT: ${{ needs.deploy-backend.result }}
          FRONTEND_CANDIDATE_RESULT: ${{ needs.candidate-frontend.result }}
          FRONTEND_RELEASE_RESULT: ${{ needs.release-frontend.result }}
          FRONTEND_READINESS_RESULT: ${{ needs.frontend-deploy-readiness.result }}
          FRONTEND_READY_TO_DEPLOY: ${{ needs.frontend-deploy-readiness.outputs.deploy }}
          FRONTEND_DEPLOY_RESULT: ${{ needs.deploy-frontend.result }}
        run: |
          set -euo pipefail
          if [ "$CI_GATE_RESULT" != "success" ]; then
            echo "CI gate did not pass; deployment completion is not evaluated."
            exit 0
          fi

          backend_requested=false
          frontend_requested=false
          if [ "$EVENT_NAME" = "push" ] && [ "$REF_NAME" = "refs/heads/main" ]; then
            backend_requested="$BACKEND_DEPLOY_CHANGED"
            frontend_requested="$FRONTEND_DEPLOY_CHANGED"
          elif [ "$EVENT_NAME" = "workflow_dispatch" ] && [ "$REF_NAME" = "refs/heads/main" ]; then
            backend_requested="$MANUAL_BACKEND"
            frontend_requested="$MANUAL_FRONTEND"
          fi

          failed=false
          if [ "$backend_requested" = "true" ] && [ "$BACKEND_DEPLOY_RESULT" != "success" ]; then
            if [ "$CONTRACT_MIGRATION" = "true" ] &&
               { [ "$EVENT_NAME" != "workflow_dispatch" ] || [ "$ALLOW_CONTRACT_MIGRATION" != "true" ]; }; then
              echo "::error::Backend deployment requires a main workflow_dispatch with deploy_backend=true and allow_contract_migration=true because the pending range contains a contract migration."
            elif [ "$BACKEND_CANDIDATE_RESULT" != "success" ]; then
              echo "::error::Backend candidate finished as $BACKEND_CANDIDATE_RESULT; no deployable artifact was produced."
            elif [ "$BACKEND_RELEASE_RESULT" != "success" ]; then
              echo "::error::Backend release finished as $BACKEND_RELEASE_RESULT; candidate verification or release artifact publication did not complete."
            else
              echo "::error::Backend deployment finished as $BACKEND_DEPLOY_RESULT after a successful release."
            fi
            failed=true
          fi
          if [ "$frontend_requested" = "true" ] && [ "$FRONTEND_DEPLOY_RESULT" != "success" ]; then
            if [ "$FRONTEND_CANDIDATE_RESULT" != "success" ]; then
              echo "::error::Frontend candidate finished as $FRONTEND_CANDIDATE_RESULT; no deployable artifact was produced."
            elif [ "$FRONTEND_RELEASE_RESULT" != "success" ]; then
              echo "::error::Frontend release finished as $FRONTEND_RELEASE_RESULT; candidate verification or release artifact publication did not complete."
            elif [ "$backend_requested" = "true" ] && [ "$BACKEND_DEPLOY_RESULT" != "success" ]; then
              echo "::error::Frontend deployment was blocked because the coordinated backend deployment finished as $BACKEND_DEPLOY_RESULT."
            elif [ "$FRONTEND_READINESS_RESULT" != "success" ] || [ "$FRONTEND_READY_TO_DEPLOY" != "true" ]; then
              echo "::error::Frontend deployment readiness was not granted (job=$FRONTEND_READINESS_RESULT, deploy=$FRONTEND_READY_TO_DEPLOY)."
            else
              echo "::error::Frontend deployment finished as $FRONTEND_DEPLOY_RESULT after a successful release."
            fi
            failed=true
          fi
          if [ "$failed" = "true" ]; then exit 1; fi
